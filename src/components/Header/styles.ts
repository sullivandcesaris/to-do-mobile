import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  container: {
    padding: 40,
    paddingBottom: 70,
    backgroundColor: '#0D0D0D',
    alignItems: 'center',
  },
})